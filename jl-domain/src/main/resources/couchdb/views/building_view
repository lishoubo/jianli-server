{
  "_id": "_design/building",
  "language": "javascript",
  "views": {
    "list": {
      /*light couch不好用,暂时看来,page查询key和value都固定死了,只能是string*/
      "map": "function(doc) { if(doc.type == 'building') { emit(doc.name, null); }  }"
    },
    "queryById": {
      /*light couch不好用,暂时看来,page查询key和value都固定死了,只能是string*/
      "map": "function(doc) { if(doc.type == 'building') { emit(doc._id, null); }  }"
    }
  }
}